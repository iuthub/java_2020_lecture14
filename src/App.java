import browser.ReadServerFile;

import javax.swing.*;

public class App {
    public static void main(String[] args) {
        ReadServerFile app = new ReadServerFile();
        app.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}
